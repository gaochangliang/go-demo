## 修改读取配置

The previous configuration are read in their own files, such as setting/setting.go file, file.go and log.go written as fixed, in fact, also need to read from the configuration file, 

now uploading images also need to add some configuration, each file to read the configuration there is a little redundant, so change to setting/setting.go file in a unified read