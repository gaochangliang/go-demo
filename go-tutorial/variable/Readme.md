## note
- 名称必须以字母开头，可以有任意数量的附加字母和数字。
- 变量名不能以数字开头。 
- 变量名不能包含空格。
- 如果一个变量名以小写字母开头，那么它只能在当前包中被访问，这被认为是未导出的变量。
- 如果变量名以大写字母开头，它可以从当前包之外的包中访问，这被认为是导出的变量。
- 如果一个名称由多个单词组成，那么第一个单词之后的每个单词都应该像这样大写：empName, EmpAddress等。
- 变量名是区分大小写的（car、Car和CAR是三个不同的变量）。