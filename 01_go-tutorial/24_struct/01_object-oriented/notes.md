Go is Object Oriented   面向对象

## (1) Encapsulation       封装
state ("fields")
behavior ("methods")
exported / un-exported

## (2) Reusability     可重复使用
inheritence ("embedded types")  集成

## (3) Polymorphism
interfaces

## (4) Overriding 覆盖
"promotion"

//////////////
Traditional OOP

Classes
-- data structure describing a type of object
-- you can then create "instances"/"objects" from the class/blue-print
-- classes hold both:
==== state / data / fields
==== behavior / methods
-- public / private

Inheritence

//////////////
In Go:
- you don't create classes, you create a type
- you don't instantiate, you create a value of a type